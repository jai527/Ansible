- name: demo on loop
  hosts: localhost
  connection: local
  
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: "hello {{ item }}"   # item is default loop variable
      loop:
        - jai
        - kushi
        - kunshi
        - rudra
        - sangeetha
        - shayamala
        - subramani